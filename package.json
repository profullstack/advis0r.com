{
  "name": "transcript-search",
  "version": "2.0.0",
  "description": "Bun CLI that discovers, indexes, and analyzes public executive communications to build a ranked, evidence-backed stock research watchlist.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "transcripts": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/server.ts",
    "cli": "bun run src/cli.ts",
    "server": "bun run src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "@libsql/client": "^0.15.0",
    "@profullstack/x402-gateway": "^0.1.0",
    "commander": "^12.1.0",
    "smol-toml": "^1.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/jsdom": "^28.0.3",
    "jsdom": "^30.0.1",
    "typescript": "^5.5.0"
  }
}
